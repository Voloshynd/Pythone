[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "python_future_collars"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
test = [
    "pytest"
]